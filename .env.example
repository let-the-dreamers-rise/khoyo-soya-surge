# Copy to .env and fill in. The app runs WITHOUT a key (deterministic heuristic
# agent), but a key unlocks Claude-powered parsing, semantic matching & PA scripts.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8
PORT=8000
